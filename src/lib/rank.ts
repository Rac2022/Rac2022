/**
 * Ranking — Phase 2.
 *
 * Planned importance score inputs:
 *   - number of sources covering the cluster (consensus signal)
 *   - source diversity (distinct domains)
 *   - recency (decay by age)
 *   - section weight from PREFERENCES
 *   - penalty if title/summary matches avoidContentTypes terms
 *
 * Left intentionally unimplemented in Phase 1.
 */
export function rankClustersPlaceholder(): void {
  // no-op for Phase 1
}
